# Weather Tracker API 🌤️

Асинхронное API для отслеживания погоды в реальном времени с использованием FastAPI, SQLAlchemy (PostgreSQL) и фоновых задач.

## 🛠 Стек технологий

* **FastAPI** — веб-фреймворк.
* **SQLAlchemy 2.0 + Alembic** — работа с БД и миграции.
* **Pydantic V2** — валидация данных и DTO.
* **APScheduler** — фоновое обновление данных о погоде.
* **Pytest** — юнит и интеграционное тестирование.
* **Docker & Docker Compose** — контейнеризация.

---

## 🚀 Быстрый запуск (Docker)

Для запуска всего приложения (API + DB + Scheduler) достаточно одной команды:

1. **Создайте файл `.env**` в корневом каталоге на основе примера:
```bash
DB_USER=postgres
DB_PASSWORD=postgres
DB_DATABASE=weather
DB_HOST=db
DB_URL=postgresql+asyncpg://postgres:postgres@db:5432/weather
WEATHER_API_KEY=your_key_here

```


2. **Запустите контейнеры**:
```bash
docker-compose up --build

```


*Приложение будет доступно по адресу: http://localhost:8000*
*Документация Swagger: http://localhost:8000/docs*

---

### 🔑 Настройка переменных окружения (.env)

Для корректной работы приложения необходимо создать файл `.env`. Обратите внимание на разницу в `DB_URL` и `DB_HOST` в зависимости от способа запуска:

| Способ запуска | Хост в DB_URL | Пример |
| :--- | :--- | :--- |
| **Полный Docker (Compose)** | `db` | `postgresql+asyncpg://user:pass@db:5432/db` |
| **Локально (IDE + dev.yml)** | `localhost` | `postgresql+asyncpg://user:pass@localhost:5432/db` |

> **Важно:** При использовании `docker-compose.dev.yml` база пробрасывает порт `5432` на ваш `localhost`, что позволяет запускать тесты и отлаживать код без пересборки контейнеров.

---

## 👨‍💻 Локальная разработка (.dev.yml)

Для удобства разработки предусмотрен отдельный сценарий `docker-compose.dev.yml`. Он запускает **только базу данных**, чтобы вы могли запускать API и тесты локально в вашей IDE.

1. **Запустите только БД**:
```bash
docker-compose -f docker-compose.dev.yml up -d

```


2. **Примените миграции**:
```bash
alembic upgrade head

```


3. **Запустите приложение**:
```bash
python -m uvicorn src.main:app --reload

```



---

## 🧪 Запуск тестов

Тесты написаны с использованием `pytest` и `pytest-asyncio`. Мы используем моки для изоляции бизнес-логики сервисов и `httpx` для тестирования эндпоинтов.

**Запуск всех тестов:**

```bash
pytest -v

```

**Запуск с отчетом о покрытии (coverage):**

```bash
pytest --cov=src tests/

```

---

## 📁 Структура проекта

* `src/core` — конфигурация, логирование и шедулер.
* `src/db` — подключение к БД и сессии.
* `src/models` — модели SQLAlchemy.
* `src/repositories` — паттерн Repository для доступа к данным.
* `src/services` — бизнес-логика (Service Layer).
* `src/schemas` — Pydantic модели (DTO).
* `tests/` — папка с тестами (Unit & API).

---

## 📋 Основные эндпоинты

* `GET /api/v1/weather/` — список актуальной погоды по всем городам.
* `GET /api/v1/weather/city/{city}` — последняя запись для конкретного города.
* `POST /api/v1/weather/refresh` — принудительное обновление данных через внешний API.
* `DELETE /api/v1/weather/{id}` — удаление записи.
